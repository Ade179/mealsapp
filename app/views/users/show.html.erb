<h1><%= @user.name %></h1>

<%= link_to 'Edit User', edit_user_path(@user) %>
<%= link_to "Destroy", user_path(@user), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>

